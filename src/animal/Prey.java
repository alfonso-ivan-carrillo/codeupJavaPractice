package animal;

public interface Prey {

     // Interface with an abstract method
     void flee();

     // The name of the interface usually contains information about what an object does, or what it is capable of
     // doing. The methods, along with the types of the parameters and return value, convey what the object is doing,
     // but not how (the implementation).

}
